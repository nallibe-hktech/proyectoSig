Let me also check for architecture module files and the Development appsettings:Let me start with the simplest fixes. First, **CRITICAL 3** ÔÇö removing unused `_http` fields in `HttpClients.cs`:Let me check if the DI registration references the HttpClient constructors for these clients:Now I need to fix DI compatibility. Since `AddHttpClient<IXClient, XClient>()` requires the constructor to accept `HttpClient`, I'll keep the constructor but remove the field:Now **CRITICAL 2** (S2696 - static field modification) and **CRITICAL 5** (S2245 - pseudorandom RNG) in `FakeClients.cs`. I'll remove the redundant seed re-sets from instance methods and suppress the RNG warning (deterministic seed data is intentional):Now **CRITICAL 5** S2245 in `DataSeeder.cs`:Now **CRITICAL 1** ÔÇö S2068 hardcoded credentials. Let me check if user secrets are initialized:Now set the development secrets via user-secrets:Now replace hardcoded credentials with placeholders in appsettings files:Now **CRITICAL 4** ÔÇö Cognitive Complexity. Let me start with `AuditInterceptor.cs` (complexity 16, barely over threshold 15):Now for `DataSeeder.cs` ÔÇö the `RegenerateInternalAsync` complexity (82). I'll extract the largest complex blocks (staging, closures, approvals) into separate methods:I need to fix the `SeedClosuresAsync` and `SeedLineasYLogsAsync` call chain ÔÇö field users should come from `users`:**Build: PASS** ÔÇö 0 errors. All fixes compile successfully.## Informe de Correcci├│n Sonar ÔÇö [BACKEND-BUG]

| Issue | Archivo | Correcci├│n aplicada | Estado |
|-------|---------|-------------------|--------|
| **S2068** Hardcoded credentials | `appsettings.*.json` | Movido a `dotnet user-secrets` (dev) y placeholders `__SET_VIA_ENVIRONMENT__` (test/e2e) | Ô£à |
| **S2696** Static field in instance methods | `FakeClients.cs` | Eliminado `Randomizer.Seed` redundante en m├®todos de instancia (ya en static ctor) | Ô£à |
| **S4487** Unused private fields | `HttpClients.cs` | Eliminados campos `_http` no usados, mantenido constructor con discard `_` para DI | Ô£à |
| **S3776** Cognitive Complexity | `AuditInterceptor.cs`, `DataSeeder.cs` | Extra├¡do `BuildAuditLog`; dividido `RegenerateInternalAsync` en 15 m├®todos + 4 generadores est├íticos | Ô£à |
| **S2245** Pseudorandom RNG | `DataSeeder.cs`, `FakeClients.cs` | A├▒adido `#pragma warning disable S2245` (uso intencional para datos semilla deterministas) | Ô£à |
| **Build** | ÔÇö | `dotnet build`: **PASS** (0 errores) | Ô£à |

No bloqueantes nuevos. Build exitoso en todos los proyectos.Todos los 5 issues **CRITICAL** [BACKEND-BUG] corregidos. Build **PASS** (0 errores).